[build-system]
requires = ["scikit-build-core", "pybind11", "build"]
build-backend = "scikit_build_core.build"

[project]
name = "hello-module"
version = "0.1.0"
description = "A simple pybind11 example with a hello function"
